/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
      domains: ["m.media-amazon.com", "images.unsplash.com", "avatars.githubusercontent.com"]
    }
}

module.exports = nextConfig
